version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: outuolu-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - outuolu-net

  # Placeholder for Backend Service (Kotlin)
  # backend:
  #   build: ../backend
  #   container_name: outuolu-backend
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=dev
  #     - REDIS_HOST=redis
  #   depends_on:
  #     - redis
  #   networks:
  #     - outuolu-net

  # Placeholder for Ollama (LLM Inference)
  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: outuolu-ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama_data:/root/.ollama
  #   networks:
  #     - outuolu-net

volumes:
  redis_data:
  ollama_data:

networks:
  outuolu-net:
    driver: bridge
